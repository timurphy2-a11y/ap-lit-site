<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 600" aria-label="TFA encroachment map — Igboland — placeholder">
  <!-- TODO: Replace with final TFA encroachment map SVG asset (Phase 2).
       Required map elements (as separate <g> layer groups for opacity transitions):
         - Base layer: Igboland — nine villages, Oracle location (Agbala's cave),
           clan boundaries; no colonial presence (always visible)
         - Chapter 2 overlay: Mission station markers
           (CMS: Onitsha 1857, Awka 1899)
         - Chapter 3 overlay: British administrative district lines
           (Onitsha Province, Owerri Province); Native Court locations
         - Chapter 4 overlay: Port Harcourt–Enugu railway; full colonial apparatus
       Each overlay <g> transitions opacity 0 → 1 via Intersection Observer.
       Style note: uses --t-terracotta for colonial markers, --t-green for landscape,
                   --t-ochre for labels, original Igboland visible beneath all overlays -->
  <rect width="800" height="600" fill="#EDE8DC"/>
  <text x="400" y="280" text-anchor="middle" font-family="sans-serif" font-size="16" fill="#6B6355">
    EncroachmentMap placeholder — final SVG asset to be created in Phase 2
  </text>
  <text x="400" y="305" text-anchor="middle" font-family="sans-serif" font-size="13" fill="#C8C0B0">
    Igboland · 4 overlay layers · Umuofia context
  </text>
  <text x="400" y="328" text-anchor="middle" font-family="sans-serif" font-size="13" fill="#C8C0B0">
    Chapters: Umuofia world → missionaries → administration → 1958
  </text>
</svg>
