---
interface Props {
  caption?: string;
  width?: 'full' | 'prose';
}
const { caption, width = 'prose' } = Astro.props;
---

<figure class={`sr-diagram-block sr-diagram-block--${width}`}>
  <slot />
  {caption && <figcaption class="sr-diagram-caption">{caption}</figcaption>}
</figure>
