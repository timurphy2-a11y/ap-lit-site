---
interface Props {
  id: string;
}

const { id } = Astro.props;
---

<button
  class="bio-link-btn"
  data-bio-id={id}
  type="button"
><slot /></button>

<style>
  .bio-link-btn {
    display: inline;
    background: none;
    border: none;
    padding: 0;
    font: inherit;
    color: var(--thread-acc, var(--u-acc, var(--acc)));
    text-decoration: underline;
    text-decoration-style: dotted;
    text-underline-offset: 3px;
    cursor: pointer;
    transition: color 0.15s;
  }
  .bio-link-btn:hover {
    color: color-mix(in oklab, var(--thread-acc, var(--u-acc, var(--acc))) 70%, var(--ink));
  }
</style>
