---
import { THREAD_ICONS } from './threadIcons';

interface Props {
  thread: string;
  size?: number;
  color?: string;
  class?: string;
}

const { thread, size = 24, color: colorProp, class: className = '' } = Astro.props;
const icon = THREAD_ICONS[thread];
const color = colorProp ?? icon?.color;
---

{icon ? (
  <svg
    width={size}
    height={size}
    viewBox={icon.viewBox}
    aria-hidden="true"
    class={`thread-icon inline-block flex-shrink-0 ${className}`}
    style={`color: ${color};`}
    set:html={icon.content}
  />
) : null}
