---
import SummerLayout from '../../../layouts/SummerLayout.astro';
import SpokePagination from '../../../components/summer-reading/SpokePagination.astro';
import { getEntry, render } from 'astro:content';

const entry = await getEntry('summerReading', 'tfa/history');
const { Content } = await render(entry);
---
<SummerLayout title={entry.data.title} section="tfa" spoke="history">
  <Content />
  <SpokePagination
    section="tfa"
    prev={{ slug: 'culture', title: 'Igbo Culture', number: 2 }}
    next={{ slug: 'figures', title: 'Key Figures', number: 4 }}
  />
</SummerLayout>
