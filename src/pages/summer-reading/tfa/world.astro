---
import SummerLayout from '../../../layouts/SummerLayout.astro';
import SpokePagination from '../../../components/summer-reading/SpokePagination.astro';
import { getEntry, render } from 'astro:content';

const entry = await getEntry('summerReading', 'tfa/world');
const { Content } = await render(entry);
---
<SummerLayout title={entry.data.title} section="tfa" spoke="world">
  <Content />
  <SpokePagination section="tfa" next={{ slug: 'culture', title: 'Igbo Culture', number: 2 }} />
</SummerLayout>
